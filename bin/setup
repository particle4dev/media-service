#!/bin/bash
# https://hub.docker.com/r/hashicorp/terraform/

source $PWD/bin/constant

docker run \
  -v $PWD/infrastructure:/data \
  -v $PWD/secret.tfvars:/secret.tfvars \
  -v $PWD/build:/lambda \
  -w /data \
  -i -t \
  hashicorp/terraform:light "${@}" -var-file="/secret.tfvars"
